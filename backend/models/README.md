# Model Artifacts

Replace these placeholder files with your real training artifacts:

- `model.joblib`
- `preprocessor.joblib`
- `feature_columns.json`

If placeholders remain, the backend will fall back to a deterministic heuristic segmentation flow so the UI can still function.
